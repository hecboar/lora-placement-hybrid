# Python dependencies for "Where Should LoRA Go?"
#
# Versions below are the EXACT versions captured at run time in
# logs/environment_snapshot.json (experiment date 2026-04-20),
# on Python 3.11.10, NVIDIA RTX 4090, CUDA 12.4, bf16.
#
# IMPORTANT: transformers / peft / trl were PRE-RELEASE (.dev0) builds installed
# from source. At the time of the experiments, support for the Qwen3.5 and
# Falcon-H1 architectures was only available in development builds. Pinning these
# exact ".dev0" strings via PyPI may not resolve; install them from the matching
# upstream git commits, or use the current stable releases that support these
# architectures. The dev strings are recorded here as the source of truth.

# --- Core deep-learning stack (exact run-time versions) ---
torch==2.4.1                 # +cu124
transformers==5.6.0.dev0     # pre-release; install from source (see note above)
peft==0.19.2.dev0            # pre-release; adapter metadata shows 0.18.2.dev0@07a1db6
trl==1.3.0.dev0              # pre-release; install from source (see note above)
datasets==4.8.4

# --- Not captured in the environment snapshot ---
# (the 8-bit Adam optimizer requires bitsandbytes; pin from your environment)
accelerate                   # replace with exact version from experiment environment
bitsandbytes                 # 8-bit Adam optimizer; replace with exact version

# --- Scientific / data ---
numpy                        # replace with exact version from experiment environment
pandas                       # replace with exact version from experiment environment
scipy                        # replace with exact version from experiment environment
scikit-learn                 # replace with exact version from experiment environment

# --- Plotting ---
matplotlib                   # replace with exact version from experiment environment

# --- Utilities ---
tqdm
jupyter
